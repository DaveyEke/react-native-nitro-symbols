///
/// HybridSymbolViewComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#import "HybridSymbolViewComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridSymbolViewSpecSwift.hpp"
#import "NitroSymbols-Swift-Cxx-Umbrella.hpp"

using namespace facebook;
using namespace margelo::nitro::nitrosymbols;
using namespace margelo::nitro::nitrosymbols::views;

/**
 * Represents the React Native View holder for the Nitro "SymbolView" HybridView.
 */
@interface HybridSymbolViewComponent: RCTViewComponentView
@end

@implementation HybridSymbolViewComponent {
  std::shared_ptr<HybridSymbolViewSpecSwift> _hybridView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridSymbolViewComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridSymbolViewComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    std::shared_ptr<HybridSymbolViewSpec> hybridView = NitroSymbols::NitroSymbolsAutolinking::createSymbolView();
    _hybridView = std::dynamic_pointer_cast<HybridSymbolViewSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  NitroSymbols::HybridSymbolViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // 1. Downcast props
  const auto& newViewPropsConst = *std::static_pointer_cast<HybridSymbolViewProps const>(props);
  auto& newViewProps = const_cast<HybridSymbolViewProps&>(newViewPropsConst);
  NitroSymbols::HybridSymbolViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update each prop individually
  swiftPart.beforeUpdate();

  // symbolName: enum
  if (newViewProps.symbolName.isDirty) {
    swiftPart.setSymbolName(static_cast<int>(newViewProps.symbolName.value));
    newViewProps.symbolName.isDirty = false;
  }
  // pointSize: optional
  if (newViewProps.pointSize.isDirty) {
    swiftPart.setPointSize(newViewProps.pointSize.value);
    newViewProps.pointSize.isDirty = false;
  }
  // weight: optional
  if (newViewProps.weight.isDirty) {
    swiftPart.setWeight(newViewProps.weight.value);
    newViewProps.weight.isDirty = false;
  }
  // tintColor: optional
  if (newViewProps.tintColor.isDirty) {
    swiftPart.setTintColor(newViewProps.tintColor.value);
    newViewProps.tintColor.isDirty = false;
  }
  // isAnimating: optional
  if (newViewProps.isAnimating.isDirty) {
    swiftPart.setIsAnimating(newViewProps.isAnimating.value);
    newViewProps.isAnimating.isDirty = false;
  }
  // scale: optional
  if (newViewProps.scale.isDirty) {
    swiftPart.setScale(newViewProps.scale.value);
    newViewProps.scale.isDirty = false;
  }
  // effect: optional
  if (newViewProps.effect.isDirty) {
    swiftPart.setEffect(newViewProps.effect.value);
    newViewProps.effect.isDirty = false;
  }
  // colors: optional
  if (newViewProps.colors.isDirty) {
    swiftPart.setColors(newViewProps.colors.value);
    newViewProps.colors.isDirty = false;
  }
  // variant: optional
  if (newViewProps.variant.isDirty) {
    swiftPart.setVariant(newViewProps.variant.value);
    newViewProps.variant.isDirty = false;
  }
  // renderingMode: optional
  if (newViewProps.renderingMode.isDirty) {
    swiftPart.setRenderingMode(newViewProps.renderingMode.value);
    newViewProps.renderingMode.isDirty = false;
  }

  swiftPart.afterUpdate();

  // 3. Update hybridRef if it changed
  if (newViewProps.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newViewProps.hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(_hybridView);
    }
    newViewProps.hybridRef.isDirty = false;
  }

  // 4. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

@end
