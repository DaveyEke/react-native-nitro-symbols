///
/// JHybridSymbolViewStateUpdater.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#include "JHybridSymbolViewStateUpdater.hpp"
#include "views/HybridSymbolViewComponent.hpp"
#include <NitroModules/NitroDefines.hpp>

namespace margelo::nitro::nitrosymbols::views {

using namespace facebook;
using ConcreteStateData = react::ConcreteState<HybridSymbolViewState>;

void JHybridSymbolViewStateUpdater::updateViewProps(jni::alias_ref<jni::JClass> /* class */,
                                           jni::alias_ref<JHybridSymbolViewSpec::javaobject> javaView,
                                           jni::alias_ref<JStateWrapper::javaobject> stateWrapperInterface) {
  JHybridSymbolViewSpec* view = javaView->cthis();

  // Get concrete StateWrapperImpl from passed StateWrapper interface object
  jobject rawStateWrapper = stateWrapperInterface.get();
  if (!stateWrapperInterface->isInstanceOf(react::StateWrapperImpl::javaClassStatic())) {
      throw std::runtime_error("StateWrapper is not a StateWrapperImpl");
  }
  auto stateWrapper = jni::alias_ref<react::StateWrapperImpl::javaobject>{
            static_cast<react::StateWrapperImpl::javaobject>(rawStateWrapper)};

  std::shared_ptr<const react::State> state = stateWrapper->cthis()->getState();
  auto concreteState = std::dynamic_pointer_cast<const ConcreteStateData>(state);
  const HybridSymbolViewState& data = concreteState->getData();
  const std::optional<HybridSymbolViewProps>& maybeProps = data.getProps();
  if (!maybeProps.has_value()) {
    // Props aren't set yet!
    throw std::runtime_error("HybridSymbolViewState's data doesn't contain any props!");
  }
  const HybridSymbolViewProps& props = maybeProps.value();
  if (props.symbolName.isDirty) {
    view->setSymbolName(props.symbolName.value);
    // TODO: Set isDirty = false
  }
  if (props.pointSize.isDirty) {
    view->setPointSize(props.pointSize.value);
    // TODO: Set isDirty = false
  }
  if (props.weight.isDirty) {
    view->setWeight(props.weight.value);
    // TODO: Set isDirty = false
  }
  if (props.tintColor.isDirty) {
    view->setTintColor(props.tintColor.value);
    // TODO: Set isDirty = false
  }
  if (props.isAnimating.isDirty) {
    view->setIsAnimating(props.isAnimating.value);
    // TODO: Set isDirty = false
  }
  if (props.isVisible.isDirty) {
    view->setIsVisible(props.isVisible.value);
    // TODO: Set isDirty = false
  }
  if (props.scale.isDirty) {
    view->setScale(props.scale.value);
    // TODO: Set isDirty = false
  }
  if (props.effect.isDirty) {
    view->setEffect(props.effect.value);
    // TODO: Set isDirty = false
  }
  if (props.colors.isDirty) {
    view->setColors(props.colors.value);
    // TODO: Set isDirty = false
  }
  if (props.variant.isDirty) {
    view->setVariant(props.variant.value);
    // TODO: Set isDirty = false
  }
  if (props.renderingMode.isDirty) {
    view->setRenderingMode(props.renderingMode.value);
    // TODO: Set isDirty = false
  }

  // Update hybridRef if it changed
  if (props.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = props.hybridRef.value;
    if (maybeFunc.has_value()) {
      std::shared_ptr<JHybridSymbolViewSpec> shared = javaView->cthis()->shared_cast<JHybridSymbolViewSpec>();
      maybeFunc.value()(shared);
    }
    // TODO: Set isDirty = false
  }
}

} // namespace margelo::nitro::nitrosymbols::views
